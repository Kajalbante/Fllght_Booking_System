spring.application.name=search-service

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/search_flight?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Ka25ja12l

# Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server port
server.port=8081

spring.security.user.name =admin
spring.security.user.password =1234

# Connect to eureka server
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# Set log level for your service package
logging.level.com.search.service=INFO

# Define log file path
logging.file.name=logs/Search-service.log

# Define log pattern (optional)
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n
